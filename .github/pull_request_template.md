## What changes

<!-- What can users (or Claude) do now, or what works better? -->

## Checklist

- [ ] `.venv/bin/python -m pytest -q tests` passes
- [ ] Tools or commands changed: `installers/gen_tools_doc.py` re-run, `docs/TOOLS.md` committed
- [ ] Works on Windows and macOS (no platform-specific paths or APIs without a fallback)
- [ ] Remote Script changes use the standard library only (Python 3.11)
- [ ] Examples, prompts or docs updated if behaviour or names changed

## Tested against a real Live?

<!-- Version, edition and OS, e.g. "Live 12.4.5 Suite, macOS 15, integration_check --scenario beat". Or "no". -->
